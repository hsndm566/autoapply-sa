# Project TODO

- [x] Resolve the `httpx` and `python-telegram-bot` dependency conflict by removing the unused Telegram client dependency while preserving existing direct HTTP notification paths; validate installation and unit tests, and open a non-merged pull request.
