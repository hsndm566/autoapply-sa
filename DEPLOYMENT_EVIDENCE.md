# Deployment Evidence

- 2026-08-12: `https://autoapply-sa-production.up.railway.app/healthz` returned HTTP 200 with `ok: true`, `engine: available`, `kill_switch_on: false`, and `external_execution_enabled: false`.
- 2026-08-12: Railway deployment `6aa2ef87-aee0-4a71-b8d1-fba27fcdb565` completed successfully with `/data/autoapply` mounted.
- The production intake route was checked immediately after the static-site merge. Its title resolved to `Start a Campaign | AutoApply SA`, but the first visual load was blank, so frontend propagation/asset availability required a follow-up verification before the deployment was considered complete.
- A production DOM check immediately after the static-site merge showed that `https://hsndm.tech/enquire/` was still loading prior hashed assets (`Enquire-CM6AdY5A.js`) and prior WhatsApp-preview copy. This confirmed the site was serving a cached or not-yet-propagated build rather than the new campaign bundle; no product claim was made until the new `Enquire-CRi04GuS.js` asset was served.
- The follow-up production check confirmed propagation: `https://hsndm.tech/enquire/` now loads `Enquire-CRi04GuS.js` and `campaignApi-CGEm3Mvt.js`. The rendered copy states that the CV is stored for a campaign and that external action remains blocked until source/package verification.
- A synthetic production campaign with the non-personal address `verification@example.invalid` and a generated test PDF successfully completed the public flow: multipart CV intake, CORS validation for `https://hsndm.tech`, campaign creation, authenticated activation, and authenticated status retrieval. The resulting campaign reached `active_readonly` with `external_execution_enabled: false`; no email, browser action, or job application occurred.
- Railway deployment `f3442c8f-2c91-48d5-80e7-f6cdbc24d39e` completed successfully after commit `a84b2d8` (Greenhouse upload-proof adapter). The adapter is not wired into a public execution route and its `ALLOW_GREENHOUSE_LIVE_SUBMISSION` default remains `false`; no portal submission, email, or source activation was performed.
