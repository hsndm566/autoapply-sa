# SESSION STATE — working memory (read first, append after every action)

> VERIFIED THIS SESSION: AutoApply SA engine, 17 operating rules all built + real-run verified + pushed to hsndm566/autoapply-sa. PLAYBOOK.md committed (portable standard). system_audit.py ran clean (alive:3, dead:0) after fixing 2 bugs (templated-URL false-dead, trailing-backtick URLs). Two hands-only items open (not failed): Azure VM Cloud Shell re-run pending user; optional Adzuna + Saudi-news keys not added (salary map logs honestly, Saudi hidden signals need a returning feed).

## Rules
- Strict verification: real data this session or say 'unverified'. No false confidence.
- Session-state: read this file first; append one line per action; keep <50 lines.

## Log
- 2026-08-07 | SAVED session-state discipline to memory + created this file. ENGINE complete (17/17 rules verified + pushed). PLAYBOOK.md pushed. Pending: Azure VM re-run (user hands), optional paid API keys.
